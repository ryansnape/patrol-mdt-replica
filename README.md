# Metro MDT Console

A fictional patrol-car MDT and Code 3 control simulator built as a static HTML/CSS/JS site.

## Preview

Open `index.html` directly or host the folder with GitHub Pages / any static host.

The siren tones use the browser Web Audio API. They work in browser previews after a user clicks a siren or horn control; browsers block autoplay audio until interaction.
